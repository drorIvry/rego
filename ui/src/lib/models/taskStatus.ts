export enum TaskStatus {
    READY        = 100,
	JOB_DEPLOYED = 200,
	PENDING      = 250,
	RUNNING      = 300,
	TIMEOUT      = 400,
	PROC_ERROR   = 410,
	APP_ERROR    = 420,
	ABORTED      = 430,
	SUCCESS      = 500,
}
